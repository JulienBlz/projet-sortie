{% extends 'base.html.twig' %}

{% block title %}ENI Sortie | Créer une sortie{% endblock %}

{% block body %}

    <h1>Créer une sortie </h1>

    {{ form_start(sortieForm) }}
    {{ form_row(sortieForm.nom) }}
    {{ form_row(sortieForm.dateHeureDebut  ) }}
    {{ form_row(sortieForm.duree) }}
    {{ form_row(sortieForm.dateLimiteInscription) }}
    {{ form_row(sortieForm.nbInscriptionsMax) }}
    {{ form_row(sortieForm.infosSortie) }}

    <div>
        {{ form_label(sortieForm.lieu) }}
        <button type="button" class="btn btn-sm btn-primary" data-toggle="modal" data-target="#locationModal">+</button>
    </div>

    {{ form_widget(sortieForm.lieu) }}
    {{ form_errors(sortieForm.lieu) }}

    {# <div>
    <button type="submit" class="btn btn-sm btn-primary" name ="enregistrer">Enregistrer</button>
    <button type="submit" class="btn btn-sm btn-primary" name="publier">Publier la sortie</button>
    <button type="submit" class="btn btn-sm btn-primary" name="supprimer">Supprimer la sortie</button>
    </div> #}

    {{ form_end(sortieForm) }}

{% endblock %}


